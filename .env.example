PORT=3000
DATABASE_URL=postgres://user:password@localhost:5432/clubapp

# Temporary MVP API key — shared secret for closed testing.
# Replace with proper auth when user accounts are introduced.
API_KEY=replace-with-a-secret-key
