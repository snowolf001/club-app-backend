require('dotenv').config({path: 'c:/repos/club-app-backend/.env'}); const { Client } = require('pg'); const c = new Client({connectionString: process.env.DATABASE_URL, ssl: {rejectUnauthorized: false}}); c.connect().then(() => c.query(\INSERT INTO schema_migrations (filename) VALUES ('001_initial_schema.sql') ON CONFLICT DO NOTHING\).then(r => { console.log('Fixed remote DB'); c.end(); })).catch(console.error);
